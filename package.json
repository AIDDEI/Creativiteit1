{
    "name": "game-creativiteit1",
    "version": "1.0.0",
    "scripts": {
        "start": "parcel src/*.html --dist-dir docs",
        "build": "parcel build src/*.html --dist-dir docs --public-url ./",
        "deploy": "npm run build && gh-pages -d dist -b master"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "pixi.js": "^6.3.0"
    },
    "devDependencies": {
        "parcel": "^2.4.0",
        "parcel-reporter-static-files-copy": "^1.3.4",
        "querystring-es3": "^0.2.1",
        "typescript": "^4.6.2"
    }
}
